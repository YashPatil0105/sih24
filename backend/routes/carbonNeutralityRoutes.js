const express = require("express");
const router = express.Router();

const {} = require("../controllers/carbonNeutralityController");

router.route("/").post();
router.route("/").post();

module.exports = router;
